# SMTP configuration for contact form emails
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=hello@tekynerds.com
SMTP_PASS=your_smtp_password

# Server port (optional, defaults to 5001)
PORT=5001
